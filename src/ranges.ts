export const curveThresholdRange = {
  min: -2,
  max: 3.5,
  step: 0.01,
  initial: 0.1,
} as const

export const shiftingDelayRange = {
  min: 0,
  max: 100,
  maxPercent: 150,
  step: 0.01,
} as const
